[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python run_web.py"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"

[[services]]
name = "calligraphy-analyzer"
