# === 核心依賴 ===
numpy
opencv-contrib-python-headless  # 需要 cv2.ximgproc（骨架化）；headless 版不含 GUI 相依，適合伺服器環境
matplotlib
pandas
scipy
seaborn
pyyaml

# === Web 伺服器（python run_web.py）===
fastapi
uvicorn[standard]
jinja2
python-multipart

# === GUI（python run_gui.py）===
# PyQt5  # 目前未使用，可選安裝

# === 圖片處理 ===
Pillow>=10.0.0

# === 其他 ===
pygame
pypinyin
